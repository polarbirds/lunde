# Which architecture to build - see $(ALL_ARCH) for options
ARCH ?= amd64

# The binary to build (just the basename)
BIN := lunde

# This repo's root import path (under GOPATH)
PKG := github.com/polarbirds/lunde

# Where to push the docker image.
REGISTRY ?= haraldfw
